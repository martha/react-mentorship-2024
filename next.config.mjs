/** @type {import('next').NextConfig} */
const nextConfig = {
    // turn strict mode off to prevent double renders
    // this is to avoid confusion
    reactStrictMode: false,
    // this will be turned on in dev in most projects so it's good to get used to debugging with it
};

export default nextConfig;
